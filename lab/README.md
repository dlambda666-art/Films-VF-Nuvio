# JustWatch release probe — LAB

This branch is an isolated experiment for FrenchPulse.

Goal:
- keep the existing DoublageVF index untouched;
- test whether public JustWatch pages expose a usable upcoming digital-release date;
- test the three current reference films;
- do not change production FrenchPulse behavior.

Important:
- this is only a probe;
- no FrenchPulse catalog integration is present here;
- no Wiflix/Flemmix access is used;
- if this works, the next step is to design the integration separately.
